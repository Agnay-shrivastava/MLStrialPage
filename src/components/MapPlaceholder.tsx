/**
 * Placeholder for map (no real map API). Stable id for automation.
 */
export function MapPlaceholder() {
  return (
    <div
      id="map-placeholder"
      className="mls-map-placeholder flex h-48 items-center justify-center rounded border border-gray-200 bg-gray-100 text-gray-500"
      aria-label="Map placeholder"
    >
      Map
    </div>
  );
}
